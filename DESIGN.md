# 추가 디자인 원칙 — 전형적인 AI 생성 UI를 피할 것

이번 리디자인에서는 단순히 "예쁘고 현대적인 UI"를 만드는 것을 목표로 하지 않는다.

특히 흔한 AI 생성 웹사이트처럼 보이지 않도록 주의한다.

다음과 같은 전형적인 패턴을 기계적으로 반복하지 말 것.

* 모든 콘텐츠를 비슷한 둥근 카드 안에 넣는 구성
* 모든 카드에 동일한 border + shadow 적용
* 지나치게 큰 border-radius
* 의미 없이 사용되는 gradient
* glassmorphism / backdrop blur 남발
* 과도한 pastel 컬러 조합
* emoji를 주요 UI icon처럼 사용하는 방식
* 섹션마다 비슷한 "제목 + 설명 + 카드" 패턴 반복
* 모든 요소를 완벽하게 중앙 정렬하는 구성
* 필요 이상으로 큰 heading
* 내용이 적은데도 여백만 지나치게 크게 사용하는 구성
* 모든 버튼을 pill 형태로 만드는 방식
* 기능마다 별도의 카드나 배지를 만드는 방식
* "AI 앱처럼 보이게 하는" 보라색 gradient 중심 디자인

UI를 만들 때 먼저 다음 질문을 한다.

> 이 요소가 실제로 별도의 container를 필요로 하는가?

필요하지 않다면 카드나 박스를 만들지 말고,
spacing, typography, divider, background hierarchy만으로 구분한다.

---

## 디자인에 의도적인 비대칭과 리듬을 사용한다

모든 요소를 동일한 크기와 간격으로 반복하지 않는다.

예를 들어:

* 중요한 정보와 부가 정보의 크기를 확실히 다르게 한다.
* 일부 영역은 카드 없이 화면 배경 위에 직접 배치한다.
* 작은 요소 여러 개보다 큰 핵심 장면 하나를 우선한다.
* 같은 종류의 콘텐츠가 아니라면 억지로 동일한 컴포넌트 모양으로 통일하지 않는다.

단, 임의적이거나 산만한 디자인은 피한다.

---

## 실제 모바일 앱처럼 생각한다

웹페이지를 모바일 폭으로 줄인 것처럼 만들지 않는다.

다음 관점으로 설계한다.

* 사용자의 엄지 이동
* 한 화면에서 가장 중요한 행동
* 화면 전환의 자연스러움
* 하단 navigation과 콘텐츠 관계
* 공부 중 화면의 몰입감
* keyboard가 올라왔을 때 입력 UX
* 작은 화면에서 정보량

특히 홈 화면을 "대시보드"처럼 만들지 않는다.

이 앱은 분석 도구가 아니라
매일 열어서 공부를 시작하는 생활형 앱이다.

---

## 제품 고유의 반복 요소를 만든다

일반적인 UI 패턴 대신 study-AI만의 작은 디자인 언어를 만든다.

예:

* 다온의 말풍선 형태
* 공부가 끝났을 때 나타나는 작은 기억 표현
* 공부 시간 표시 방식
* 캐릭터 주변의 은은한 조명
* 방과 공부 기록에서 반복되는 작은 motif

단, 장식용 요소를 많이 만드는 것이 아니라
1~2개의 고유한 시각 언어를 여러 화면에서 일관되게 재사용한다.

---

## reference 없는 임의 디자인을 최소화한다

"modern", "clean", "cute", "premium" 같은 추상적인 단어만으로 디자인 결정을 하지 않는다.

각 변경마다 이유가 있어야 한다.

예:

나쁜 이유:
"더 현대적으로 보이기 위해 gradient를 추가했다."

좋은 이유:
"공부 시작 CTA와 보조 행동 사이의 hierarchy가 약해서 primary action의 대비를 높였다."

디자인 변경은 항상 UX 목적과 연결한다.

---

## 구현 후 자기 검토

UI 수정 후 아래를 확인한다.

1. 카드가 너무 많지 않은가?
2. border-radius가 과도하지 않은가?
3. 한 화면에 포인트 컬러가 너무 많지 않은가?
4. 어디서 본 AI SaaS 화면처럼 보이지 않는가?
5. 공부 시작 CTA가 가장 먼저 보이는가?
6. 캐릭터보다 공부 기능이 중심인가?
7. 정보가 없는 공간을 억지로 decoration으로 채우지 않았는가?
8. 동일한 컴포넌트를 기계적으로 반복하지 않았는가?

문제가 있으면 다시 단순화한다.
